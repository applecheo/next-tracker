const SubHeader = ({ title }: any) => (
  <header className="flex-none flex h-16 bg-gray-100 border-t border-gray-700 px-4 items-center">
    <h1 className="font-semibold text-lg">{title}</h1>
  </header>
);

export default SubHeader;
